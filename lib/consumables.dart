import 'package:flutter/material.dart';

class Consumables extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Coming Soon!'),
    );
  }
}